# Copy to `.env` and fill in.

# --- Database -----------------------------------------------------------
# Neon (recommended): create a free project at neon.tech, then paste BOTH
# the pooled and direct connection strings below. The "-pooler.c-5" segment
# in the host distinguishes them.
DATABASE_URL="postgresql://USER:PASSWORD@PROJECT-pooler.c-5.REGION.aws.neon.tech/DB?sslmode=require&pgbouncer=true&connect_timeout=15"
DIRECT_URL="postgresql://USER:PASSWORD@PROJECT.c-N.REGION.aws.neon.tech/DB?sslmode=require&connect_timeout=15"

# Local Postgres alternative (Homebrew or Postgres.app):
# DATABASE_URL="postgresql://garage:garage@localhost:5432/garage?schema=public"
# DIRECT_URL="postgresql://garage:garage@localhost:5432/garage?schema=public"

# --- Server -------------------------------------------------------------
PORT=3000
NODE_ENV=development

# --- Auth ---------------------------------------------------------------
# Generate strong secrets for prod with: openssl rand -base64 48
JWT_ACCESS_SECRET="dev-access-secret-change-me"
JWT_REFRESH_SECRET="dev-refresh-secret-change-me"
ACCESS_TOKEN_TTL_MIN=15
REFRESH_TOKEN_TTL_DAYS=7

# --- Storage ------------------------------------------------------------
# Local filesystem in dev; switch to S3/MinIO in prod (not yet implemented).
UPLOADS_DIR=./uploads

# --- LLM (Gemini) -------------------------------------------------------
# Gemini handles ALL three AI features:
#   - Receipt OCR (multimodal — image goes straight to Gemini, no Vision API needed)
#   - AI-phrased service reminders (§16-B)
#   - "Explain this prediction" modal (§16-C)
# Free tier covers v1 volumes (1M tokens/day, 15 req/min).
# Get a key at https://aistudio.google.com/apikey
LLM_PROVIDER=gemini                  # gemini | openai | anthropic (only gemini implemented)
GEMINI_API_KEY=
LLM_MODEL=gemini-1.5-flash
LLM_TIMEOUT_MS=15000                 # hard timeout per call (image OCR is slower)

# --- Demo mode ----------------------------------------------------------
# When "true", OCR / reminder messages / predict-explainer all return
# hardcoded fallback responses without calling Gemini. Useful for offline
# demos. Switch to "false" once GEMINI_API_KEY is filled in above.
DEMO_MODE=false
